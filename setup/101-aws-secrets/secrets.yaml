apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: aws-secrets-test
spec:
  provider: aws
  parameters:
    region: us-east-1
    objects: |
      - objectName: "eks-secrets"
        objectType: "secretsmanager"